---
title: Work Experience
description: Brian DeVries' work experience
layout: page.njk
eleventyNavigation:
  key: Experience
  order: 600
--- 

{% for job in workHistory | reverse %}
  <div>
    <p>{{job.title}}</p>
    <p><em>{{job.startDate | datemonthyear}} - {% if job.endDate === null %}Present{% else %}{{job.endDate | datemonthyear}}{% endif %}</em></p>
    <p>{{job.description}}</p>
  </div>
  {% if not loop.last %}<hr/>{% endif %}
{% endfor %}
